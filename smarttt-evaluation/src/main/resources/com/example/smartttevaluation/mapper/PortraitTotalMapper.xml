<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.smartttevaluation.mapper.PortraitTotalMapper">


    <insert id="insert">
        INSERT INTO ai_portrait_total (course_id,classroom_id,total_nums)
        VALUE(#{courseId},#{classroomId},#{totalNums})
    </insert>

    <update id="update">
        UPDATE ai_portrait_total
        SET total_nums = #{totalNums}
        WHERE course_id = #{courseId}
          AND classroom_id = #{classroomId}
    </update>
    <select id="selectByCondition" resultType="java.lang.Integer">
        SELECT id FROM ai_portrait_total
        WHERE course_id = #{courseId}
          AND classroom_id = #{classroomId}
    </select>
    <select id="getNumByCondition" resultType="java.lang.Integer">
        SELECT total_nums FROM ai_portrait_total
        WHERE course_id = #{courseId}
          AND classroom_id = #{classroomId}
    </select>
</mapper>