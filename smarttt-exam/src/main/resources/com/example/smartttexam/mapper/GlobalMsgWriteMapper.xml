<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.smartttexam.mapper.GlobalMsgWriteMapper">
    <insert id="insert" parameterType="com.example.smartttexam.schedule.entity.EduMessage">
        INSERT INTO global_msg (type, classroomId, indexId, action, context, state, remark, createTime)
        VALUES (#{type}, #{classroomId}, #{indexId}, #{action}, #{context}, #{state}, #{remark}, #{createTime})
    </insert>
</mapper>
